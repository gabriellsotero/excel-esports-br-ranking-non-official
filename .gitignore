# Editor-generated CSS sourcemaps (we hand-write CSS)
*.css.map

# Node (only used locally for tooling/verification)
node_modules/
package-lock.json

# Python
__pycache__/

# Source CSVs (rankings.csv, class.csv) are committed so the
# "Build data.json" GitHub Action can regenerate data.json on push.
